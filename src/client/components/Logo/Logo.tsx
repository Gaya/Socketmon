import { FunctionComponent } from 'react';

import logo from './socketmon-logo.png';

const Logo: FunctionComponent = () => (
  <img alt="Socketmon" style={{ height: 32 }} src={logo} />
);

export default Logo;
